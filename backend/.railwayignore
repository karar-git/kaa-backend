# What `railway up` sends. data/ is deliberately excluded: the Dockerfile
# fetches results/, static/ and model/ from the GitHub repository at build
# time, so the upload is the code only and finishes on any connection. Raw
# frames live on the service's volume (see README, "Shipping the raw frames").
__pycache__/
*.pyc
.venv/
.venv312/
.env
smoke_test.py
data/
